{
  "name": "ppu-pdf",
  "version": "1.0.4",
  "main": "dist/index.js",
  "module": "index.ts",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts",
    "build:npm": "bun build --target=node ./index.ts --outfile=dist/index.js && bun run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.types.json",
    "postbuild": "rimraf tsconfig.types.tsbuildinfo",
    "example": "bun run src/pdf.example.ts",
    "copy-pdfjs-worker": "bash ./scripts/copy-pdfjs-worker.sh",
    "build:exec": "bun run copy-pdfjs-worker && bun build --production index.ts --compile --external 'pdf.worker.min.mjs' --outfile pdf-reader",
    "release": "bun run copy-pdfjs-worker && bun build:npm"
  },
  "files": [
    "dist/*.js",
    "dist/*.d.ts"
  ],
  "keywords": [
    "pdf-reader",
    "text-extraction",
    "pdf-rag",
    "bbox",
    "pdf",
    "pdf-typescript",
    "bun",
    "pdf-digital",
    "pdfjs"
  ],
  "author": "snowfluke",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/PT-Perkasa-Pilar-Utama/ppu-pdf.git"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "pdfjs": "^2.5.3",
    "pdfjs-dist": "^4.10.38"
  }
}
